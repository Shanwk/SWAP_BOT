const PRIVATE_KEY='Your privatekey here'

const TOKEN_ADDRESS=''; // change to Token address you want to target
const AMOUNT=1
const LEVEL=0.1

module.exports = {
    PRIVATE_KEY,
    TOKEN_ADDRESS,
    AMOUNT,
    LEVEL
};
